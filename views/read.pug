html
    head
        title 게시글
    body
        h2 #{data.title}

        p 작성 일자 : #{data.day}
        p 작성자 : #{data.author}
        p #{data.content}


        a(href="/read/"+(data.idx-1))
            - if(data.idx > 1)
                input(type="button" value="이전글")
            - else
                input(type="button" value="이전글" disabled)

        a(href="/home")
            input(type="button" value="목록")

        a(href="/read/"+(data.idx + 1))
            - if(data.idx < cnt)
                input(type="button" value="다음글")
            - else 
                input(type="button" value="다음글" disabled)